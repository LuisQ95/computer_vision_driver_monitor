conda create --name computer_vision_sleep_py python=3.11 -y
conda activate computer_vision_sleep_py

# Instalar librerias
pip install -r requirements.txt

# Si es necesario desinstalar el env
conda deactivate
conda remove -n computer_vision_sleep_py --all -y

# Para eliminar cache
pip cache purge

# Si quieres subirlo a tu github

git init

git add .

git commit -m "Initial commit"

git branch -M main

git remote add origin <YOUR_GITHUB_REPOSITORY_URL> (Por ejm: https://github.com/LuisQ95/computer_vision_driver_monitor/)

git push -u origin main